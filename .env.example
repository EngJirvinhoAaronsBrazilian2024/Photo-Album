# Firebase configuration is loaded from firebase-applet-config.json.
# No additional environment variables are required for the client app.
#
# For Vercel deployment, no environment variables need to be set.
# The Firebase config is bundled at build time from firebase-applet-config.json.
